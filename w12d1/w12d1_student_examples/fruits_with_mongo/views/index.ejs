<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1>Fruits index page</h1>
        <h4><a href="/fruits/new">Add New Fruit</a></h4>
        <ul>
            <% for(let i = 0; i < fruits.length; i++){ %>
                <li>
                    The 
                    <a href="fruits/<%= fruits[i].id %>"><%=fruits[i].name; %></a> 
                    is  <%=fruits[i].color; %>.
                    <% if(fruits[i].readyToEat === true){ %>
                        It is ready to eat
                    <% } else { %>
                        It is not ready to eat
                    <% } %> <br />
                    <a href="/fruits/<%=fruits[i].id%>/edit">EDIT</a>
                    <!-- ADD DELETE FORM HERE-->
                    <form action="/fruits/<%=fruits[i].id%>?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE" />
                    </form>
                </li>
            <% } %>
        </ul>
    </body>
</html>