// start with initialized variable 0
// if variable is less than or equal to 10 keep looping through code and add 2 to the variable
//add the sum of each loop to the sum variable

sum = 0;
for (i = 0; i <= 10; i += 2) {
    sum += i;
}

console.log(sum);
